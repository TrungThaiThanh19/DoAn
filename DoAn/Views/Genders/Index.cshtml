@model IEnumerable<DoAn.Models.GioiTinh>

@{
    ViewData["Title"] = "Danh sách giới tính";
    string keyword = ViewBag.Keyword as string ?? "";
}

<h2 class="mb-4 text-primary">⚧️ Danh sách giới tính</h2>

<div class="row mb-3">
    <div class="col-md-6">
        <form method="get" asp-action="Index" class="input-group">
            <input type="text" name="keyword" class="form-control" placeholder="🔍 Tìm theo tên giới tính..." value="@keyword" />
            <button type="submit" class="btn btn-outline-primary">Tìm</button>
        </form>
    </div>
    <div class="col-md-6 text-end">
        <a asp-action="Create" class="btn btn-success rounded-pill px-4">+ Thêm giới tính</a>
    </div>
</div>

<div class="table-responsive">
    <table class="table table-bordered table-hover align-middle text-center">
        <thead class="table-dark">
            <tr>
                <th>Mã giới tính</th>
                <th>Tên giới tính</th>
                <th>Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ID_GioiTinh</td>
                        <td>@item.Ten_GioiTinh</td>
                        <td>
                            <a asp-action="Details" asp-route-idGioiTinh="@item.ID_GioiTinh" class="btn btn-sm btn-outline-info rounded-pill">Xem thêm</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="3" class="text-muted">Không tìm thấy giới tính nào.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
